// Re-export from Mongoose schema (replaces TypeORM entity)
export { TokenHolderSnapshotDocument } from '../schemas/token-holder-snapshot.schema';
export type { TokenHolderSnapshot } from '../schemas/token-holder-snapshot.schema';
