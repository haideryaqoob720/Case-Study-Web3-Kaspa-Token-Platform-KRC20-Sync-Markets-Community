// Re-export from Mongoose schema (replaces TypeORM entity)
export { TokenExchangeDocument } from '../schemas/token-exchange.schema';
export type { TokenExchangeEntity } from '../schemas/token-exchange.schema';
