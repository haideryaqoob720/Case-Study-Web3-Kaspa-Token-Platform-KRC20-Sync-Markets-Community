// Re-export from Mongoose schema (replaces TypeORM entity)
export { TransactionDocument } from '../schemas/transaction.schema';
export type { TransactionEntity } from '../schemas/transaction.schema';
